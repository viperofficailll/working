import { Button } from "./components/Button"; 

function App() {
  return (
    <>
      <Button varient="primary" text="Share" onClick={() => {}} size="sm" />
        <Button varient="secondary" text="Add content" onClick={()=>{}} size="lg"></Button>
    </>
  );
}

export default App;
